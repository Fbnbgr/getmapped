# Copy to .env and fill in your SRU credentials
SRU_USER=your_username
SRU_PASS=your_password

# Optional: delay between SRU requests in milliseconds (default: 250)
SRU_DELAY_MS=250

DB=data/maps.db
